// SPDX-License-Identifier: MIT

#include <cstdlib>
#include "base/macros.h"

int main(int argc, char* argv[], char* env[]) {
    UNUSED(argc);
    UNUSED(argv);
    UNUSED(env);

    return EXIT_SUCCESS;
}
